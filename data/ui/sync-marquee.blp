using Gtk 4.0;

template $VanitySyncMarquee : Box {
  styles [ "sync_marquee"]

  ScrolledWindow viewport {
    hexpand: true;

    focusable: false;
    sensitive: false;
    vscrollbar-policy: never;

    hadjustment: Adjustment scroll_adjust {
    };
  }
}
